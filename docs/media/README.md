`renderer-smoke-*.png` are contact sheets from `tools/renderer-smoke` (headless Chromium, software GL); the `c` sheet follows the layered cloud decks (nine scenes). App screenshots for the README are captured from the first deployed build (see docs/QA_PLAN.md release smoke checklist).
